import ConfirmView from 'olx\src\components\ConfirmView.vue'

const router = createRouter({

    routes: [
        {
            path: '/confirmRegistrations',
            name: confirmRegistration,
            component: ConfirmView
        }
    ]

})